# LinkFlow Frontend

The LinkFlow frontend is a React and Vite application. Its complete setup, environment, API, deployment, and architecture documentation is in the repository [README](../README.md).

```powershell
npm install
npm run dev
npm run lint
npm run build
```

Copy `.env.example` to `.env` and set `VITE_API_BASE_URL` before starting the application.
